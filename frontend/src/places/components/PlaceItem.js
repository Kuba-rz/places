import React from 'react'

import './PlaceItem.css'

const PlaceItem = () => {

    if (props.places.length === 0) {
        return (
            <p>This user has no places added...</p>
        )
    }
    return (
    )
}

export default PlaceItem